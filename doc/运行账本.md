# Nexus 运行账本

> v2 起（2026-09-15）记录每次能力上线的日期/用途/成本/备注，以及 **7 天自用验证**结果。
> 规则（执行计划 §0）：每条细条上线后连续 7 天真实使用才算交付；连续 7 天没用 = 砍掉或重做。

## 记录

| 日期 | 条目 | 用途 | 成本 | 备注 |
|---|---|---|---|---|
| 2026-09-15 | S1 代码段完成（QQ 渠道底座） | NapCat 容器 + OneBot adapter + 白名单/路由/会话映射 + 前端 chip/徽标 | 开发期零运行成本 | commit e4d72cb |
| 2026-09-15 | 评测成本分层（--tier core/full） | 日常全量 85→49 trial（token 约省一半） | 一次性 0 | commit 2a20bbb；日常缺省 core，里程碑 --tier full |
| 2026-09-15 | **S1 部署完成 + 验收通过** | QQ 私聊双向问答上线（owner 2577324935 ↔ 机器人 2730475638「aklのsmoke」） | 按次 LLM 调用（默认链路一次约 6-8k token；/kb 约 6k） | 验收实录：私聊问答 ✅、/kb 转交前缀+来源归属（《Nexus项目进度文档》）✅、/write Web 引导 ✅、断连演练（stop→gauge 0→恢复重连 gauge 1）✅、双会话隔离落库（qq:{uid} / qq:{uid}:/cmd）✅。部署坑：①Docker Hub 直连超时，经 docker.1ms.run 拉取 retag；②NapCat 配置目录 /app/napcat/config 不挂载则容器重建丢反向 WS 配置（已补 volume ./data/napcat-config）；③首次 stop 后 NTQQ 登录态失效需重扫（已配 ACCOUNT=2730475638 快速登录，后续重启免扫码，待日常观察） |
| 2026-09-16 | **S2 代码段 + 部署完成（定时任务与推送）** | APScheduler 调度器（3 默认 job：30min 巡检 state_change / 每日 8:00 巡检日报 / 每日 21:00 KB 日报）+ http_check/push_message 工具（目标锁定 env）+ egress 唯一出口复用 send_qq_message + jobs API + 前端 Jobs 页签 + 评测 job 适配器（零外发硬断言） | 单测 362 / 集成 61 全绿（dummy key 模式）；eval 零外发演练经真实 LLM 验证（agent 真实调用 push_message 被抑制，pushed_to=suppressed(eval)）；jobs 评测用例「完全」（TSR 100%，run 20260916-101422）；单次巡检 job 运行 ≈ 4-6k token | 部署坑两条：①Next.js 根路径 307 无 Location 头跟不动，MONITOR_TARGETS 的 frontend 目标必须用 /chat；②Docker Desktop 崩溃硬杀容器打掉 NTQQ 登录态，napcat 重扫码一次恢复（ACCOUNT 已配但无 NAPCAT_QUICK_PASSWORD 时快速登录回退二维码） |
| 2026-09-16 | **S2 技术验收通过（渠道恢复后）** | eval 零外发演练（agent 真实调 push_message 被抑制）✅；调度准点触发（10:42:50 → succeeded 基线不推送）✅；state_change 检测 + 断连降级（run 64 检出 frontend down→up，渠道离线如实落账 failed(channel_offline)）✅；**真实 QQ 推送**（run 79 ops_daily_report → agent 主动 push_message 三目标全 UP 日报，pushed_to=qq，owner 已收到）✅ | 真实推送一次 2571 token | **插曲（记入 S1 渠道稳定性观察）**：Docker Desktop 崩溃硬杀 → NTQQ 登录态失效 → 重扫触发 QQ 账号风控（错误码 168'账号存在安全风险'）→ 用户手机 QQ 完成安全验证后重扫码成功恢复，全程约 5 小时。教训：napcat 异常掉线后别连续重试扫码，先在手机 QQ 上恢复账号再扫 |
| 2026-09-16 | **S2 down→up 告警演练通过（验收门 §5.9.3）** | 18:30 停 frontend 容器 → 18:44 调度巡检**无人值守**检出 `frontend: up→down` → QQ 收到告警（pushed_to=qq，run 81：NameResolutionError 初诊'疑似 DNS/服务发现故障'+建议动作+其余目标状态）→ 18:46 拉起 → 19:14 下轮巡检检出 `down→up` → QQ 收到恢复通知（run 82：status=200/98ms + 初诊'服务已就绪'）。两轮均为调度自动触发（manual=False），state_change 策略只在变化时推送，全程无噪音 | 两轮各 ≈2.6k token | 告警带初诊（§3.1.3 替代品检验的答案）实证：不是 UptimeRobot 式裸状态翻转通知，而是带错误详情/方向性初诊/建议动作/全局对照的消息 |
| 2026-09-17 | **S3 定向变更：原"钉钉双向接入"砍掉，改为钉钉告警备用通道（S3'）** | 用户真实动机"监听公司群公告"经核实不可行（非企业管理员、机器人进不了公司群、公告不 @ 收不到、无个人消息 API、无成熟第三方协议端且公司 IM 不碰）；"自建企业自用问答"无真实场景（7 天 ≥10 次提问必不过，防玩具纪律砍）；保留价值 = QQ 风控时告警兜底。im_pipeline 抽象重构与抽象完型验收移至 S4 飞书阶段 A（计划 §6/§7 已改写；双向方案 SDK 事实存档 git 923d5d3） | 0 | 计划 §6 占位替换当日两次：先双向细化稿、后定向变更版（均有 commit） |
| 2026-09-17 | **S3' 用户裁定搁置** | 用户原话"做了也没意义，暂时搁置"——不建机器人、不做验收演练、不推进 7 天验证；代码保留**休眠**（webhook 未配置 = 备推关闭 = 零行为差异，QQ 主通道不变） | 0 | 启用成本 ≈ 2 分钟（建群加机器人 → 填 env → up -d）。解锁条件（任一）：QQ 渠道再发真实失联/风控事件造成告警丢失；用户重启意愿。记账于当日 S3' 代码段条目之后 |
| 2026-09-17 | **S3' 代码段：钉钉告警备推上线（代码完成，真实演练待凭据）** | dingtalk_push 服务（官方 demo 同款 HMAC-SHA256 加签 + text 消息 + 1500 分段；零新依赖）+ egress 备推编排（backup/always/off，主 failed(...) 才兜底；eval 早返回保证 QQ/钉钉双零外发）+ job_runs.pushed_to_backup 落账（alembic 0011）+ push_message 工具回话如实说明兜底 | 单测 384 / 集成 62 全绿（+22：加签向量/mode 三态/eval 双抑制/离线兜底/零外发双出口断言） | **本机库正式采纳 alembic 迁移链**（此前 schema 一直走 create_all 无 alembic_version 表；stamp 0010 → upgrade 0011，新列已生效）；DINGTALK_PUSH_* 未配置 = 备推关闭，QQ 主通道行为与落账语义零变化；**用户当日裁定搁置（见上条），验收演练取消** |
| 2026-09-17 | **S4 阶段 A：im_pipeline 抽象重构完成（纯重构零新渠道，停在验收门）** | onebot_adapter 内嵌入站管线平移为渠道无关 `channels/im_pipeline.py`（InboundMessage{channel,sender_id,text,is_group,owners,reply} 归一化 + handle_inbound 主入口 + 白名单/不可信包裹/路由 v0/HITL/会话映射/串行排队/执行回复全链）；adapter 收敛为薄壳（传输/原生事件解析/owner env/回复闭包/在线状态——渠道差异封闭清单）；新增渠道注册表 `channels/registry.py`，GET /v1/channels、/metrics gauge、前端 chip 全部改遍历注册表（第二渠道接入消费方零改动）；QQ_SESSION_QUEUE_MAX 更名 IM_SESSION_QUEUE_MAX（纯代码默认值，本机 .env 未显式设置，零迁移成本） | 单测 396（+12，含**抽象守卫**：im_pipeline 源码扫描零渠道 token——阶段 A 验收条款的自动化形态）/ 集成 62 / 评测 core 层 TSR 100%（run 20260917-142103，**新栈部署后跑**）；镜像重建部署 health 200、NapCat 自动重连恢复（connected=1）、调度器三任务 next 正确 | 锁语义按计划 §7 指定改为发送者级 `{channel}:{sender_id}`（两条更安全方向的微调）：①同一发送者默认链路与 /cmd 子链路原各自持锁可并发、现串行（跨渠道不互锁有专测）；②群聊锁由按群改为按发送者（自用单 owner 无感）。会话键 `qq:{uid}`、run_id 前缀 `qq-`、egress 唯一出口 send_qq_message 均不变；/metrics 指标名 onebot_connected* 不变（help 文案微调）；split_long_message 移入 im_pipeline 纯函数化（QQ 侧仍传 QQ_MESSAGE_MAX_LEN，行为不变） |

## 7 天自用验证

| 条目 | 开始日期 | 状态 | 结果（保留/砍掉/重做） |
|---|---|---|---|
| S1 QQ 双向问答 | 2026-09-15 | **进行中**（目标：一周 ≥10 次真实提问） | — |
| S2 定时任务与推送 | 2026-09-16 | **进行中**（8:00/21:00 日报连续到达；down→up 告警演练 ✅ 2026-09-16 完成，见记录表） | — |

### S2 日报到达记录（2026-09-16 → 09-23，连续 7 天）

| 日期 | 8:00 巡检日报 | 21:00 KB 日报 | 备注 |
|---|---|---|---|
| 2026-09-16 | —（当日 8 点早于部署） | ✅ 21:00 准点，'今日无新增'（run 86，用户确认收到；1176 token） | down→up 演练同日通过（run 81/82） |
| 2026-09-17 | ❌ 未运行（非调度故障）：宿主机夜间停机——job1 巡检末次 09-17 00:44，backend ~10:40 随 Docker 重启拉起，8:00 调度点整机不在运行；重启后 next_run 正确顺延 09-18 08:00，11:10 巡检恢复准点 | 待今晚验证 | 计划 §5.9.3"本机不睡眠前提，账本如实记录"情形；渠道 onebot_connected 恢复 =1 |
