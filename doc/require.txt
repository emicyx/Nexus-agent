面向群体


定位
多功能，可自行按需配置的私人AI Agent助手，开发重点在于高度客制化，简单自定义
需要干什么：用户可以在前端界面与agent简单对话；可以自定义编辑agent-team相关内容；可以指派agent执行任务

agent-team需要哪些功能：
可以对文件进行读写操作
可以对网页进行解析操作
可以替代用户执行部分操作（带扩展）
#（暂时不考虑实现）可以对图片等多模态数据进行识别




采用框架：
Agent-team
crewai + fast api + TypeScript (Next.js) 轻量控制前端          #后端将 Agent 的配置（Prompt、参数、图结构）抽象为 JSON/YAML 数据（Config as Code），前端通过 CopilotKit 编辑这些数据，后端接收数据后动态重载 Agent 实例。


功能
1.简单chat界面
2.Agent-team模式(前端自定义task，crew，agent，skill)




DB Schema 设计图：确认包含 agent_tools 中间表、tasks_config JSONB 字段、多角色 messages 表。
SSE 事件字典：前后端约定好所有 Event Type（如 agent_thinking, tool_call, hitl_request, error）及对应的数据结构。
HITL 状态机流转图：明确 PENDING -> APPROVED/REJECTED/TIMEOUT 的完整生命周期。
异常处理与熔断策略：明确 Token 超限、API 超时、LLM 幻觉时的降级和报错提示方案。
Tool 异步规范：团队达成共识，所有自定义 Tool 必须遵循异步非阻塞原则。


todo:（2026-08-21 状态刷新，详细方案见 doc/roadmap.md）
1.优化agent执行速度，目前执行速度慢，原因待排查（部分完成：工具懒加载、事件循环 offload patch、评估模型降级 qwen-turbo；剩余见 roadmap P2-5）
2.用户对话持久化（已完成：chat_sessions/chat_messages 落 PG，前端会话列表）
3.验证HITL功能（已修复：PENDING→APPROVED/REJECTED/TIMEOUT 状态机 + 审批卡吸顶/轮询，e2e 证据 testing/e2e/evidence_after/smoke_hitl.json）
4.继续构建crew生态（进行中：已有 researcher_writer/knowledge_qa/safety_check/safety_review/team_orchestrator/web_ingest_crew 六个种子 crew）
5.agent返回内容都是一小段的文字，我认为需要更详细的返回（后端已完成 task_completed/delegation 事件，前端渲染未接 — roadmap P1-1）
6.loop-agent实现，可能要重新建一个crew团队（未开始 — roadmap P2-5）

2026-08-21 全量代码评审新增的高优先级待办（详见 doc/roadmap.md）：
- P0：文件工具路径沙箱、全站鉴权、SSRF/日志泄密、部署面收紧、factory 并发缺陷
- P1：前端补齐 SSE 事件渲染、提交测试+CI、上传限流、Redis KEYS 替换