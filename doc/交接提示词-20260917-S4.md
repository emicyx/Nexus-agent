# Nexus v2 开发交接（2026-09-17，S4 飞书开工 / S1-S2 验证收尾）

你是本仓库的执行开发会话。唯一执行依据是《doc/harness重构-执行计划.md》（下称"计划"），
先完整读一遍再动手；与调研文档冲突时以计划为准。工作纪律：默认只做当前细条，
做完停在验收门等我验证；防玩具三纪律与安全不变量（计划 §3）任何一条不让步。

## 当前状态（截至交接时，HEAD = 332acc4）

- R0 重构清理 / 评测成本分层：✅ 完成
- S1 QQ 双向问答：✅ 技术验收通过；7 天自用验证进行中（2026-09-15 起，≥10 次真实提问，记运行账本）
- S2 定时任务与推送：✅ 技术验收全绿；**7 天验证进行中（2026-09-16 → 09-23）**
  - 三默认 job 在跑：30min 巡检（state_change）、8:00 巡检日报、21:00 KB 日报；记录表在运行账本「S2 日报到达记录」
  - 09-17 8:00 未运行 = 宿主机夜间停机（非调度故障，已如实记录）；日报到达请每日核销，用户报"没收到"就查 `GET /v1/jobs/{id}/runs` 排障
  - **断连演练（§5.9.4，停 napcat 一晚）还没做**，验证期内任意一晚执行；09-23 到期 → 账本记结论、计划里 S1/S2 标记关闭
- **S3 已两度改道、最终搁置（勿动勿删）**：原"钉钉双向接入"砍（公司群监听经核实不可行：非企业管理员进不了群、公告不 @ 收不到、无个人消息 API）；改做的 S3' 钉钉告警备推已**代码完成并部署**（commit 3c14cef：dingtalk_push + egress 备推编排 + alembic 0011，单测 384/集成 62/CI 三 job 全绿），但用户当日裁定"做了也没意义，暂时搁置"——代码休眠（`DINGTALK_PUSH_WEBHOOK` 空 = 备推关闭 = 零行为差异），不需要回滚；启用 ≈2 分钟（解锁条件见计划 §6 注记）
- **本机库已正式采纳 alembic 迁移链**（此前 schema 一直走 create_all、无 alembic_version 表；现 head=0011）：新迁移正常写 versions + upgrade head 即可；全新环境由 create_all 建全列无需手动迁移
- CI 三 job 全绿；分支 **v2 = master_server = 332acc4 同头已推**；日常开发在 v2，要触发 CI 需同步推 master_server（`git push origin v2:master_server`）
- 工作区干净（tmp-crew87-backup.json 是有意留的备份，勿动）

## 本次任务（按顺序）

1. **开工核对**：`git log --oneline -8` + `docker ps` + 计划全文通读；与本交接一致才继续。
2. **S4 阶段 A：im_pipeline 抽象重构（先做这个）**——纯重构、零新渠道、无需任何凭据：
   - 把 onebot_adapter 内嵌入站管线抽成渠道无关 `backend/app/channels/im_pipeline.py`，**设计要点已完整写在计划 §7**（InboundMessage 归一化结构 / handle_inbound 管线 / adapter 差异封闭清单 / 渠道注册表 / 阶段 A 验收），照做即可
   - 行为不变 + 全量回归全绿（单测/集成/评测 core 层）+ diff 审查（im_pipeline 无 QQ 特有引用）→ **停在验收门等我确认**，才进阶段 B
   - 钉钉双向方案的 SDK 调研存档在 git `923d5d3`（InboundMessage/封闭清单同款打法，可参考结构）
3. **S4 阶段 B 开工门（动手前先过我这一关）**：
   - ①自用场景确认：问我"会真的每天在飞书里跟它说话吗"——没有肯定回答就不开工（S3' 当日搁置的教训，计划 §7 开工门条款）
   - ②飞书侧准备（我确认后才做）：个人免费建"企业" + 企业自建应用（`lark-oapi` SDK WebSocket 长连接订阅 `im.message.receive_v1`，免公网回调），把 `FEISHU_APP_ID/SECRET` 给会话
   - ③lark-oapi SDK 事实在会话内核实（调研文档只有结论级事实 §1.3/§2.5）；已知与钉钉的关键差异：飞书回复走 OpenAPI 需要 **access_token 管理**（tenant_access_token 刷新），不像钉钉 sessionWebhook 免 token——线程模型/凭据管理要按此设计
   - ④细化写进计划 §7 替换占位，经我确认后实施
4. **并行日常（验证线，不阻塞开发）**：8:00/21:00 日报到达核销（用户报没收到 → 排障）；断连演练挑一晚（停 napcat → 次日 up -d → 验证 job_runs 落账与重连续推）；09-23 到期收尾 S1/S2。

## 环境速查（本机坑，全部实测）

- **本机测试 venv：`.venv/`**（crewai 1.9.3 + pytest；系统 python 是 crewai 1.1.0 跑不了）
  - 单测：`QWEN_API_KEY=sk-local-dummy .venv/Scripts/python.exe -m pytest testing/unit -q`
  - 集成（本机需 PG/Redis 在跑）：`QWEN_API_KEY=<真实key> POSTGRES_DSN=postgresql://nexus:nexus@localhost:5432/nexus REDIS_URL=redis://localhost:6379/0 .venv/Scripts/python.exe -m pytest testing/integration -q`
- 评测：日常缺省 core 层（`testing/eval/runner.py`）；零外发演练独立跑：`.venv/Scripts/python.exe -m testing.eval.job_adapter --job ops_daily_report`
- Docker Hub 直连超时：镜像拉取走 docker.1ms.run retag；**改 .env 后必须 `docker compose up -d` 重建（restart 不重读 env_file，实测坑）**
- **Docker Desktop 崩溃**：恢复链 = 启动 `D:\docker\Docker Desktop.exe` → `docker compose up -d`（postgres/redis/backend/frontend 无 restart policy 会全停）。崩溃可能打掉 napcat NTQQ 登录态：6099 WebUI 重扫码（token 查 `docker logs myagent-napcat-1 | grep Token`）；**连续重试扫码触发 QQ 风控（168），先在手机 QQ 完成安全验证再扫**（09-16 实录，恢复约 5 小时）
- napcat WebUI：http://localhost:6099（仅 loopback）；MONITOR_TARGETS 的 frontend 目标必须用 `/chat`（Next.js 根路径 307 无 Location 头会误报 DOWN）
- **alembic 迁移（本机库已采纳，head=0011）**：容器内跑需带必填 env——`docker exec myagent-backend-1 sh -c "cd /app && QWEN_API_KEY=x alembic upgrade head"`；本机直接跑：`cd backend && QWEN_API_KEY=sk-local-dummy POSTGRES_DSN=postgresql://nexus:nexus@localhost:5432/nexus ../.venv/Scripts/python.exe -m alembic upgrade head`（settings 校验缺 env 会报 pydantic Field required）
- SEED_DEMO_DATA=true 已进本机 .env；生产默认 false + 容器手动 `ensure_seed()`
- QQ 渠道状态：`curl localhost:8000/v1/channels`；job 管理：`/v1/jobs` CRUD + 前端 /config → Jobs 页签；**钉钉备推状态看 `GET /v1/jobs/{id}/runs` 的 pushed_to_backup 列**（当前恒 null = 休眠正常）

## 关键文档指针

- 执行计划（唯一依据，S4 规格在 §7 含阶段 A 设计要点）：doc/harness重构-执行计划.md
- 调研文档（只查不执行）：doc/harness化重构-调研与方案.md（飞书事实 §1.3）
- 部署手册：doc/上线部署手册.md（S1 §十四 / S2 §十五 / S3' §十六）
- 运行账本（7 天验证记录 + S3 改道/搁置全实录）：doc/运行账本.md
- 钉钉双向方案存档（SDK 事实，结构可参考）：git show 923d5d3
