# Nexus v2 开发交接（2026-09-17，S2 收尾 / S3 开工）

你是本仓库的执行开发会话。唯一执行依据是《doc/harness重构-执行计划.md》（下称"计划"），
先完整读一遍再动手；与调研文档冲突时以计划为准。工作纪律：默认只做当前细条，
做完停在验收门等我验证；防玩具三纪律与安全不变量（计划 §3）任何一条不让步。

## 当前状态（截至交接时）

- R0 重构清理 / 评测成本分层：✅ 完成
- S1 QQ 双向问答：✅ 技术验收通过；7 天自用验证进行中（2026-09-15 起，≥10 次真实提问，记运行账本）
- **S2 定时任务与推送：✅ 技术验收全绿**（代码 ec579dd / 验收 1d6ae7f / down→up 演练 1c78884 / CI 修复 dd1751d）
  - 三默认 job 在跑：30min 巡检（state_change，只在 up/down 变化时推送）、8:00 巡检日报、21:00 KB 日报
  - 已实证：eval 零外发（agent 真实调 push_message 被抑制 + 评测用例「完全」）、无人值守 down→up 告警（停/起 frontend 各收到一条带初诊告警）、真实 QQ 推送、断连如实落账 failed(channel_offline)、熔断与 eval 不计数
  - **7 天日报到达验证进行中（2026-09-16 → 09-23）**：记录表在运行账本「S2 日报到达记录」；断连演练一晚（§5.9.4）还没正式做，可在验证期内任意一晚执行
  - **CI 三 job 全绿**（2026-09-17 起集成 job 首次通过；此前历史全败是 CI 缺 .env，已修：workflow 从 .env.example 生成）
- 分支：**v2 已推 GitHub**（= master_server HEAD dd1751d，两分支同头）；开发在 master_server 或 v2 均可，推远端触发 CI 只认 master/master_server
- master_server 分支干净，全部已 commit（tmp-crew87-backup.json 是有意留在工作区的备份，勿动）

## 本次任务（按交接时点自动判定）

1. **先核对 7 天验证状态**（运行账本 S2 日报到达记录表 + S1 验证行）：
   - 已到 2026-09-23 且记录齐全 → 账本记结论、计划里 S1/S2 标记关闭，进入 2；
   - 未到期 → 当日工作：用户报"某次没收到"就查 job_runs 排障（`GET /v1/jobs/{id}/runs`）；
     断连演练没做就挑一晚做（stop napcat 一晚 → up -d → 验证 job_runs 落账与重连续推）；
     其余时间做 S3 规格细化（写进计划 §6 替换占位，不动代码）。
2. **Slice 3：钉钉双向接入**（计划 §6，细化经我确认后实施）：
   - `dingtalk-stream` SDK WebSocket 反向推送（免公网；凭据 env DINGTALK_APP_KEY/SECRET）
   - `backend/app/channels/dingtalk_adapter.py` 与 onebot_adapter 同构；**验收重点是抽象**：
     渠道差异（身份字段/消息格式/回复 API）全封闭在 adapter 内，核心链路（白名单→会话映射→路由 v0→run_crew_chat→回复）零渠道特有分支
   - 推送侧复用 egress（目标扩展钉钉 webhook 需谨慎：QQ_ALERT_TARGET 的"目标只来自 env"纪律同样适用）
   - 可选（我点头才做）：告警双推 QQ 主/钉钉备
3. 开工前两件事：①`git log --oneline -8` 与 `docker ps` 核对状态与本交接一致；②向我要钉钉企业内部应用凭据（AppKey/Secret，先在钉钉开放平台建好）。

## 环境速查（本机坑，全部实测）

- **本机测试 venv：`.venv/`**（crewai 1.9.3 + pytest；系统 python 是 crewai 1.1.0 跑不了）
  - 单测：`QWEN_API_KEY=sk-local-dummy .venv/Scripts/python.exe -m pytest testing/unit -q`
  - 集成（本机需 PG/Redis 在跑）：`QWEN_API_KEY=<真实key> POSTGRES_DSN=postgresql://nexus:nexus@localhost:5432/nexus REDIS_URL=redis://localhost:6379/0 .venv/Scripts/python.exe -m pytest testing/integration -q`
- 评测：日常缺省 core 层（`testing/eval/runner.py`，30 用例含 jobs 零外发用例）；
  零外发演练独立跑：`.venv/Scripts/python.exe -m testing.eval.job_adapter --job ops_daily_report`
- Docker Hub 直连超时：镜像拉取走 docker.1ms.run retag；**改 .env 后必须 `docker compose up -d` 重建（restart 不重读 env_file，实测坑）**
- **Docker Desktop 崩溃**（今天两次）：恢复链 = 启动 `D:\docker\Docker Desktop.exe` → `docker compose up -d`
  （postgres/redis/backend/frontend 无 restart policy 会全停；napcat 除非配置否则重启）。
  崩溃可能打掉 napcat NTQQ 登录态：6099 WebUI 重扫码（token 查 `docker logs myagent-napcat-1 | grep Token`）；
  **连续重试扫码会触发 QQ 账号风控（错误 168），先在手机 QQ 上完成安全验证再扫**（2026-09-16 实录，恢复约 5 小时）
- napcat WebUI：http://localhost:6099（仅 loopback）；MONITOR_TARGETS 的 frontend 目标必须用 `/chat`
  （Next.js 根路径 307 无 Location 头，跟不动会误报 DOWN）
- SEED_DEMO_DATA=true 已进本机 .env；生产默认 false + 容器手动 `ensure_seed()`（S2 seed 依赖它）
- QQ 渠道状态：`curl localhost:8000/v1/channels`；job 管理：`/v1/jobs` CRUD + 前端 /config → Jobs 页签

## 关键文档指针

- 执行计划（唯一依据）：doc/harness重构-执行计划.md
- 调研文档（只查不执行）：doc/harness化重构-调研与方案.md（钉钉/飞书事实 §1.3）
- 部署手册：doc/上线部署手册.md（S1 §十四 / S2 §十五：env 清单/验收清单/回滚）
- 运行账本（7 天验证记录处）：doc/运行账本.md
