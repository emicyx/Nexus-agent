[pytest]
asyncio_mode = auto
testpaths = unit integration
python_files = test_*.py *_test.py smoke_*.py
markers =
    e2e: 真实 LLM 端到端 smoke 测试（需 QWEN_API_KEY，用 --run-e2e 显式开启）
addopts = -p no:cacheprovider
